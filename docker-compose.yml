services:
  gitsync:
    build: 
      context: .
      dockerfile: ./src/Dockerfile
    container_name: gitsync
    env_file:
      - .env
    volumes:
      - ./data/repos:/data/repos
    restart: unless-stopped
